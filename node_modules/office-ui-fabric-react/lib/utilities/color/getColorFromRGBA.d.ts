import { IRGB, IColor } from 'office-ui-fabric-react/lib/utilities/color/interfaces';
/** Converts an RGBA color to a color object (alpha defaults to 100). */
export declare function getColorFromRGBA(rgba: IRGB): IColor;
